/**
 * Settings — every adjustable value in one place.
 *
 * Placeholder. Routing, the navigation entry, the icon and the copy keys are all
 * wired already, so this file is the only thing left to write.
 */
export default function SettingsPage({ apiBase }: { apiBase: string }) {
  void apiBase;
  return null;
}
