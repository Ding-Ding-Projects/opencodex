/**
 * First-run onboarding wizard.
 *
 * Placeholder. It is already mounted in App.tsx beside the snackbar host and the
 * dim sum card, and its copy keys exist — it decides for itself whether this is a
 * first run and renders nothing otherwise.
 */
export default function OnboardingWizard({ apiBase }: { apiBase: string }) {
  void apiBase;
  return null;
}
